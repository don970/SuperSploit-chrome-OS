                    Help section for show command
      shows the values supplied in the arg position. The only
      valid arguments are the program variables such as R_HOST,
      L_HOST, ect. Can take more then one variable. show details
      shows a full detail page for the exploit

      [Usage]:
        show [arg-a] [arg-b]

      [Example]:
        show R_HOST L_HOST
        show details
